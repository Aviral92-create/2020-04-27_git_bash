

Hello all

Staging:  "What is going on"
First attempt below:
Change b/w local and git at diff. place does not matter


Git error if same line commit difference in local and rep.
Fine
5) Change making in remote rep.

##Branches
git checkout branchname
git switch -new way

Talkimng about stashing with arguments

##Branch assignment
git stash: temp saves current state as a commit so you can checkout or switch
git stash apply to apply the last stash from the stack 

##PUll requests(Online merge):

git push origin<Branch name>: pushes branch to the remote
                              This is where PR is created (Online)
			      You merge the PR and also the branch by accepting and merging the PR

Don't forget to clean up yr branches

#REbase
Add change at new position


